{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "buildCommand": "echo 'Fresh Build 1.0.2' && npm install --legacy-peer-deps && npm run build"
    },
    "deploy": {
        "startCommand": "npm run start"
    }
}